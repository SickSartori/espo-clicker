{
  "name": "espo-clicker",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js && npm run build:v3",
    "build:legacy": "node build.js",
    "build:watch": "node build.js --watch",
    "build:dev": "node build.js --dev && npm run build:v3:dev",
    "build:v3": "vite build",
    "build:v3:dev": "vite build --mode development",
    "build:v3:watch": "vite build --watch",
    "dev:v3": "vite",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "budget": "node scripts/bundle-budget.js",
    "lighthouse": "lhci autorun",
    "bump:major": "node bump-version.js major && npm run build",
    "bump:minor": "node bump-version.js minor && npm run build",
    "bump:patch": "node bump-version.js patch && npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "esbuild": "^0.24.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "break_eternity.js": "^2.1.3",
    "lucide": "^1.14.0",
    "lz-string": "^1.5.0",
    "motion": "^12.38.0",
    "pixi.js": "^8.18.1"
  }
}
